import { redirect } from '@sveltejs/kit';

export const load = async (event) => {
    //console.log(event)
    
    /*
    if (!session) {
        redirect(307, '/signin');
    }

    return {
        session
    };
    */
};
