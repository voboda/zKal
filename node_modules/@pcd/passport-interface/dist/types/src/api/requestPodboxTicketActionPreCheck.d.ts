import { Credential } from "../Credential";
import { ActionConfigResponseValue } from "../RequestTypes";
import { PodboxTicketAction } from "../TicketAction";
import { APIResult } from "./apiResult";
/**
 * Prior to performing an action such as check-in on a ticket, retrieve a list
 * of supported actions, if any. If the credential does not authorize the
 * performance of actions on this ticket, the return value will indicate this.
 *
 * {@link credential} is a Semaphore Signature of a payload that is a
 * `JSON.stringify`-ed {@link CredentialPayload}.
 *
 * Never rejects. All information encoded in the resolved response.
 */
export declare function requestPodboxTicketActionPreCheck(preCheckUrl: string, credential: Credential, action: PodboxTicketAction, ticketId: string, eventId: string): Promise<PodboxTicketActionPreCheckResult>;
export type PodboxTicketActionPreCheckResult = APIResult<ActionConfigResponseValue>;
//# sourceMappingURL=requestPodboxTicketActionPreCheck.d.ts.map