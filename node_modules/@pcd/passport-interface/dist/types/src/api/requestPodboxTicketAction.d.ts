import { Credential } from "../Credential";
import { PodboxTicketActionResponseValue } from "../RequestTypes";
import { PodboxTicketAction } from "../TicketAction";
import { APIResult } from "./apiResult";
/**
 * Asks the server to perform an action in relation to a ticket issued by
 * Podbox, such as check-in or badge gifting.
 * {@link credential} is a Semaphore Signature of a payload
 * that is a `JSON.stringify`-ed {@link CredentialPayload}.
 *
 * Never rejects. All information encoded in the resolved response.
 */
export declare function requestPodboxTicketAction(checkInUrl: string, credential: Credential, action: PodboxTicketAction, ticketId: string, eventId: string): Promise<PodboxTicketActionResult>;
export type PodboxTicketActionResult = APIResult<PodboxTicketActionResponseValue>;
//# sourceMappingURL=requestPodboxTicketAction.d.ts.map