import { OneClickLoginResponseValue } from "../RequestTypes";
import { APIResult } from "./apiResult";
/**
 * This function allows a user to log in with a one-click login process.
 * It sends the necessary data to the backend and handles the response.
 *
 * Never rejects. All information encoded in the resolved response.
 */
export declare function requestOneClickLogin(zupassServerUrl: string, email: string, code: string, commitment: string, encryptionKey: string): Promise<LoginResult>;
export type LoginResult = APIResult<OneClickLoginResponseValue>;
//# sourceMappingURL=requestOneClickLogin.d.ts.map