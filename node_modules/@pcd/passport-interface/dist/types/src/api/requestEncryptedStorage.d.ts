import { DownloadEncryptedStorageResponseValue } from "../RequestTypes";
import { APIResult, NamedAPIError } from "./apiResult";
/**
 * Downloads, but does not decrypt, a user's end-to-end encrypted backup
 * of their PCDs and other user data. The server never learns the user's
 * encryption key.
 *
 * Never rejects. All information encoded in the resolved response.
 */
export declare function requestEncryptedStorage(zupassServerUrl: string, blobKey: string, knownRevision?: string): Promise<EncryptedStorageResult>;
export type EncryptedStorageResult = APIResult<DownloadEncryptedStorageResponseValue, NamedAPIError>;
//# sourceMappingURL=requestEncryptedStorage.d.ts.map