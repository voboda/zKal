import { CheckTicketInByIdError, CheckTicketInByIdRequest, CheckTicketInByIdResponseValue } from "../RequestTypes";
import { APIResult } from "./apiResult";
/**
 * Tries to check the user in. This is called by the Zupass client when
 * a 'superuser' of a particular event wants to check in a Devconnect
 * attendee into the event.
 *
 * Sends the ticket ID. See {@link requestCheckIn} for an alternative
 * API which sends a serialized PCD.
 *
 * Never rejects. All information encoded in the resolved response.
 */
export declare function requestCheckInById(passportServerUrl: string, postBody: CheckTicketInByIdRequest): Promise<CheckTicketInByIdResult>;
export type CheckTicketInByIdResult = APIResult<CheckTicketInByIdResponseValue, CheckTicketInByIdError>;
//# sourceMappingURL=requestCheckInById.d.ts.map