import { ServerProofRequest } from "./RequestTypes";
export declare function hashProveRequest(req: ServerProofRequest): string;
export interface PendingPCD {
    /**
     * Current status of the pending PCD using PendingPCDStatus enum.
     */
    status: PendingPCDStatus;
    /**
     * The type of PCD that a server is producing a proof for.
     */
    pcdType: string;
    /**
     * A hash of the ProveRequest using hashProveRequest. Stored to avoid
     * people re-sending the same request many times and clogging
     * the proving queue.
     */
    hash: string;
}
export declare enum PendingPCDStatus {
    QUEUED = "queued",
    PROVING = "proving",
    COMPLETE = "complete",
    ERROR = "error",
    NONE = "none"
}
export declare function isSettledPendingPCDStatus(status: PendingPCDStatus): boolean;
//# sourceMappingURL=PendingPCDUtils.d.ts.map