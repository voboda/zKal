"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.requestEncryptedStorage = void 0;
const url_join_1 = __importDefault(require("url-join"));
const apiResult_1 = require("./apiResult");
const makeRequest_1 = require("./makeRequest");
/**
 * Downloads, but does not decrypt, a user's end-to-end encrypted backup
 * of their PCDs and other user data. The server never learns the user's
 * encryption key.
 *
 * Never rejects. All information encoded in the resolved response.
 */
async function requestEncryptedStorage(zupassServerUrl, blobKey, knownRevision) {
    return (0, makeRequest_1.httpGet)((0, url_join_1.default)(zupassServerUrl, "/sync/v3/load"), {
        onValue: async (resText) => ({
            value: JSON.parse(resText),
            success: true
        }),
        onError: apiResult_1.onNamedAPIError
    }, { blobKey, knownRevision });
}
exports.requestEncryptedStorage = requestEncryptedStorage;
