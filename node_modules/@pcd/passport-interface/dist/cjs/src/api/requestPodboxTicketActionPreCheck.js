"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.requestPodboxTicketActionPreCheck = void 0;
const url_join_1 = __importDefault(require("url-join"));
const makeRequest_1 = require("./makeRequest");
/**
 * Prior to performing an action such as check-in on a ticket, retrieve a list
 * of supported actions, if any. If the credential does not authorize the
 * performance of actions on this ticket, the return value will indicate this.
 *
 * {@link credential} is a Semaphore Signature of a payload that is a
 * `JSON.stringify`-ed {@link CredentialPayload}.
 *
 * Never rejects. All information encoded in the resolved response.
 */
async function requestPodboxTicketActionPreCheck(preCheckUrl, credential, action, ticketId, eventId) {
    return (0, makeRequest_1.httpPostSimple)((0, url_join_1.default)(preCheckUrl), async (resText) => {
        return {
            success: true,
            value: JSON.parse(resText)
        };
    }, {
        credential,
        action,
        ticketId,
        eventId
    });
}
exports.requestPodboxTicketActionPreCheck = requestPodboxTicketActionPreCheck;
