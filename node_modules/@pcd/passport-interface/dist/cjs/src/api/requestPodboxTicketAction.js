"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.requestPodboxTicketAction = void 0;
const url_join_1 = __importDefault(require("url-join"));
const makeRequest_1 = require("./makeRequest");
/**
 * Asks the server to perform an action in relation to a ticket issued by
 * Podbox, such as check-in or badge gifting.
 * {@link credential} is a Semaphore Signature of a payload
 * that is a `JSON.stringify`-ed {@link CredentialPayload}.
 *
 * Never rejects. All information encoded in the resolved response.
 */
async function requestPodboxTicketAction(checkInUrl, credential, action, ticketId, eventId) {
    return (0, makeRequest_1.httpPostSimple)((0, url_join_1.default)(checkInUrl), async (resText) => {
        return {
            success: true,
            value: JSON.parse(resText)
        };
    }, {
        credential,
        action,
        ticketId,
        eventId
    });
}
exports.requestPodboxTicketAction = requestPodboxTicketAction;
