"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.requestConfirmationEmail = void 0;
const url_join_1 = __importDefault(require("url-join"));
const makeRequest_1 = require("./makeRequest");
/**
 * Asks the server to send the given email address a confirmation email
 * that contains a token which can be used to login/reset an account.
 *
 * In dev mode, can return the token rather than sending it to the email,
 * to speed up the development loop.
 *
 * Never rejects. All information encoded in the resolved response.
 */
async function requestConfirmationEmail(zupassServerUrl, email, commitment, force) {
    return (0, makeRequest_1.httpPostSimple)((0, url_join_1.default)(zupassServerUrl, "/account/send-login-email"), async (resText) => resText === "OK"
        ? { value: undefined, success: true }
        : {
            value: JSON.parse(resText),
            success: true
        }, {
        email,
        commitment,
        force: force ? "true" : "false"
    });
}
exports.requestConfirmationEmail = requestConfirmationEmail;
