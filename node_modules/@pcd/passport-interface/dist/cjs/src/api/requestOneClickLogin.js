"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.requestOneClickLogin = void 0;
const url_join_1 = __importDefault(require("url-join"));
const makeRequest_1 = require("./makeRequest");
/**
 * This function allows a user to log in with a one-click login process.
 * It sends the necessary data to the backend and handles the response.
 *
 * Never rejects. All information encoded in the resolved response.
 */
async function requestOneClickLogin(zupassServerUrl, email, code, commitment, encryptionKey) {
    return (0, makeRequest_1.httpPostSimple)((0, url_join_1.default)(zupassServerUrl, "/account/one-click-login"), async (resText) => ({
        value: JSON.parse(resText),
        success: true
    }), {
        email,
        code,
        commitment,
        encryptionKey
    });
}
exports.requestOneClickLogin = requestOneClickLogin;
