"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useFetchUser = void 0;
const react_1 = require("react");
const requestUser_1 = require("./api/requestUser");
function useFetchUser(zupassServerUrl, isZuzalu, uuid) {
    const [user, setUser] = (0, react_1.useState)(null);
    const [error, setError] = (0, react_1.useState)(null);
    const [loading, setLoading] = (0, react_1.useState)(false);
    (0, react_1.useEffect)(() => {
        const doLoad = async () => {
            if (uuid === undefined) {
                setUser(null);
                setError(null);
                setLoading(false);
                return;
            }
            setLoading(true);
            const userResult = await (0, requestUser_1.requestUser)(zupassServerUrl, uuid);
            setLoading(false);
            if (userResult.success) {
                setUser(userResult.value);
            }
            else {
                if (userResult.error.userMissing) {
                    setError(`no user with id '${uuid}'`);
                }
                else {
                    setError(userResult.error.errorMessage);
                }
            }
        };
        doLoad();
    }, [isZuzalu, zupassServerUrl, uuid]);
    return { user, error, loading };
}
exports.useFetchUser = useFetchUser;
